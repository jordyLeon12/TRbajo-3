Algoritmo CalculoExpresion
    Definir resultado, numerador, denominador Como Real
    // Calcula el numerador de la fracción
    numerador <- 2 * (4 - 10 + 8)
    // Calcula el denominador de la fracción
    denominador <- 2 * 36 * (1 / 2)
    // Calcula el resultado de la expresión
    resultado <- numerador / denominador
    // Muestra el resultado
    Escribir "El resultado de la expresión es: ", resultado
FinAlgoritmo
